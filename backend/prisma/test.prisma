generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:PolockProg@09@localhost:5432/postgres"
}

model Test {
  id Int @id @default(autoincrement())
}
